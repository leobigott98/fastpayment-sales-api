const express = require("express");
const router = express.Router();
const pool = require("../db");
const fs = require('fs')

//Download image from server
const downloadFondoBlanco = () => {

  
};

const downloadRecurso = () => {
    
};

module.exports = {
    downloadFondoBlanco,
    downloadRecurso
}